'use strict';

// Method includes есть ли элемент в массиве
{
  const arr = [10, 20, 30, 40];
  console.log(arr.includes(20));
}

{
  const arr = [10, 20, 30, 40];
  console.log(arr.includes('a'));
}
