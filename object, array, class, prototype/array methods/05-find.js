'use strict';

// Method find Возвращает первый элемент массива, который удовлетворил условие

{
  const arr = [10, 20, 30, 40];
  console.log(arr.find(x => x > 25));
}

{
  const arr = [10, 20, 30, 40];
  console.log(arr.findIndex(x => x > 25));
}

